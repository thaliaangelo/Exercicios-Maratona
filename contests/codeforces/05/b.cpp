#include <bits/stdc++.h>

using namespace std;

int main(){
    /*
    int t; cin >> t;

    while(t--){
        int n, a, b, c; cin >> n >> a >> b >> c;
        int ans = 0, sum = 0;
        while(sum < n){
            sum += a;
            ans++;
            if (sum >= n) break;
            sum += b;
            ans++;
            if (sum >= n) break;
            sum += c;
            ans++;
        } 

        cout << ans << endl;
    }
    */

   cout << 1000000000%3 << endl;
}