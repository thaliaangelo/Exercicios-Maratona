#include <bits/stdc++.h>

using namespace std;

int main(){
    vector<map<string, int>> lista;
    int N, T;
    for (int i = 0; i < N; i++){
        int x; cin >> x;
        string s; cin >> s;
        lista[s].
    } 
}